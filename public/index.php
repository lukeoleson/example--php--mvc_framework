<?php
    // Since index.php is the entry point, it needs to require all the other files for our app, which we will require in the bootstrap.php file
    require_once '../app/bootstrap.php';

    // init core library
    // * instantiate the Core library
    $init = new Core;
